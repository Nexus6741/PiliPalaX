# 鸿蒙防休眠插件使用指南

## 插件信息

- **插件名称**: wakelock_ohos
- **版本**: 1.0.0
- **GitHub 仓库**: https://github.com/Nexus6741/wakelock_ohos
- **功能**: 在鸿蒙系统上防止设备在视频播放等场景下自动休眠

## 上传到 GitHub

### 方式一：使用脚本（推荐）

1. 运行上传脚本：
```bash
upload_wakelock_ohos.bat
```

### 方式二：手动上传

```bash
cd packages/wakelock_ohos

# 初始化 Git
git init

# 添加远程仓库
git remote add origin https://github.com/Nexus6741/wakelock_ohos.git

# 拉取远程仓库（如果有 README）
git pull origin main --allow-unrelated-histories

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit: HarmonyOS wakelock plugin v1.0.0"

# 推送
git push -u origin main
```

## 在项目中使用

### 1. 添加依赖

在 `pubspec.yaml` 中添加：

```yaml
dependencies:
  wakelock_ohos:
    git:
      url: https://github.com/Nexus6741/wakelock_ohos.git
      ref: main
```

### 2. 安装依赖

```bash
flutter pub get
```

### 3. 使用示例

#### 方式一：使用 WakelockManager（推荐）

项目中已经封装了 `WakelockManager`，自动处理鸿蒙和其他平台：

```dart
import 'package:PiliPalaX/utils/wakelock_manager.dart';

// 启用防休眠
await WakelockManager.instance.enable();

// 禁用防休眠
await WakelockManager.instance.disable();

// 检查是否已启用
bool isEnabled = await WakelockManager.instance.isEnabled();

// 检查是否支持
bool isSupported = await WakelockManager.instance.isSupported();
```

#### 方式二：直接使用插件

```dart
import 'package:wakelock_ohos/wakelock_ohos.dart';

// 启用防休眠（永久持锁）
await WakelockOhos.enable(timeout: -1);

// 启用防休眠（3秒后自动释放）
await WakelockOhos.enable(timeout: 3000);

// 禁用防休眠
await WakelockOhos.disable();

// 检查是否持有锁
bool isHolding = await WakelockOhos.isHolding();

// 检查系统是否支持
bool isSupported = await WakelockOhos.isSupported();
```

## 技术实现

### 核心 API

使用鸿蒙官方推荐的 `window.setWindowKeepScreenOn()` API：

```typescript
// 启用屏幕常亮
await windowClass.setWindowKeepScreenOn(true);

// 禁用屏幕常亮
await windowClass.setWindowKeepScreenOn(false);
```

### 权限配置

插件已自动配置所需权限，无需手动添加。

### 文件结构

```
wakelock_ohos/
├── lib/
│   └── wakelock_ohos.dart          # Dart 接口
├── ohos/
│   ├── src/main/
│   │   ├── ets/components/plugin/
│   │   │   └── WakelockOhosPlugin.ets  # 原生实现
│   │   └── module.json5            # 模块配置
│   ├── index.ets                   # 入口文件
│   ├── hvigorfile.ts              # 构建配置
│   └── build-profile.json5        # 构建配置
├── pubspec.yaml                    # 包配置
├── README.md                       # 说明文档
└── .gitignore                      # Git 忽略配置
```

## 项目集成

### 播放器集成

在 `lib/plugin/pl_player/controller.dart` 中已集成：

```dart
// 播放状态监听器中自动控制
videoPlayerController!.stream.playing.listen((event) {
  if (event) {
    // 播放时启用防休眠
    WakelockManager.instance.enable();
  } else {
    // 暂停时禁用防休眠
    WakelockManager.instance.disable();
  }
});
```

### 功能特点

✅ **自动管理**: 播放时自动启用，暂停时自动禁用
✅ **跨平台**: 自动识别鸿蒙和其他平台
✅ **无侵入**: 通过播放状态监听器自动控制
✅ **双重保护**: 监听器 + 方法调用双重保护机制

## 测试验证

### 1. 播放测试
- 打开视频播放
- 等待超过系统息屏时间
- 验证屏幕保持常亮 ✅

### 2. 暂停测试
- 暂停视频播放
- 等待系统息屏时间
- 验证屏幕正常息屏 ✅

### 3. 恢复播放测试
- 恢复视频播放
- 验证屏幕再次保持常亮 ✅

## 注意事项

1. **规范使用**: 仅在必要场景（视频播放、导航等）使用，退出场景后应禁用
2. **系统检测**: 系统可能检测非规范使用并恢复自动灭屏
3. **平台兼容**: 自动处理鸿蒙和其他平台的差异
4. **调试日志**: 生产环境已注释所有调试日志

## 更新日志

### v1.0.0 (2024-12-17)
- ✅ 初始版本发布
- ✅ 实现基于 window.setWindowKeepScreenOn 的防休眠功能
- ✅ 支持永久持锁和超时释放
- ✅ 集成到播放器控制器
- ✅ 添加跨平台管理器
- ✅ 完整的错误处理和状态管理

## 相关文件

- `lib/utils/wakelock_manager.dart` - 跨平台管理器
- `lib/plugin/pl_player/controller.dart` - 播放器集成
- `packages/wakelock_ohos/` - 插件源码
- `upload_wakelock_ohos.bat` - 上传脚本

## 许可证

MIT License

## 作者

Nexus6741

## 问题反馈

如有问题，请在 GitHub 仓库提交 Issue：
https://github.com/Nexus6741/wakelock_ohos/issues

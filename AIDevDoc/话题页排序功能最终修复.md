# 话题页排序功能最终修复

## 问题描述
话题页排序功能存在严重问题：
1. 进入页面时默认显示热门列表，但UI没有选中热门
2. 点击热门时，列表刷新但显示最新内容
3. 再次点击热门仍显示最新内容
4. 点击最新时列表变灰色，没有内容
5. 需要多次点击才能正确显示对应排序的内容

## 根本原因分析

### 1. 排序状态初始化问题
原来的逻辑在每次 `queryData()` 时都可能重置排序状态：
```dart
if (sortBy.value == 0) {
  sortBy.value = data.topicSortByConf?.showSortBy ?? 0;
}
```
这导致用户选择的排序被反复覆盖。

### 2. 排序状态与UI不同步
- 服务器返回的默认排序与UI显示的选中状态不匹配
- 用户点击排序后，状态更新但可能被后续请求覆盖

## 修复方案

### 1. 添加初始化标志
```dart
bool _isInitialized = false;
```
用于标记是否已经完成初始化，避免重复设置默认排序。

### 2. 修复排序初始化逻辑
```dart
// 只在第一次初始化时设置默认排序
if (!_isInitialized && data.topicSortByConf != null) {
  sortBy.value = data.topicSortByConf?.showSortBy ?? 0;
  _isInitialized = true;
}
```

### 3. 使用 onReload() 方法
```dart
void onSort(int newSortBy) {
  sortBy.value = newSortBy;
  onReload();
}
```
参考 PiliPlus 的实现，使用 `onReload()` 而不是直接调用 `queryData()`。

## 修复的关键点

### 1. 状态保护
- 使用 `_isInitialized` 标志保护排序状态
- 只在真正的初始化时设置默认排序
- 避免服务器响应覆盖用户选择

### 2. 响应式更新
- `sortBy` 使用 `RxInt` 确保UI能响应状态变化
- 排序按钮在 `Obx` 中，能自动更新选中状态

### 3. 正确的重载逻辑
- 使用 `onReload()` 确保完整的数据重载
- 清理状态并重新请求数据

## 修复后的工作流程

### 初始加载
1. 进入页面，`sortBy = 0`，`_isInitialized = false`
2. 请求数据，获得排序配置
3. 设置默认排序，`_isInitialized = true`
4. UI显示正确的选中状态

### 用户切换排序
1. 用户点击排序按钮
2. 设置新的 `sortBy` 值
3. 调用 `onReload()` 重新加载数据
4. 保持用户选择的排序状态

### 后续请求
1. 加载更多数据时，不再修改 `sortBy`
2. 保持用户选择的排序状态
3. UI状态与数据状态保持同步

## 技术要点

1. **状态管理**：使用标志位保护用户选择
2. **响应式编程**：确保UI与数据状态同步
3. **生命周期管理**：正确处理初始化和后续更新
4. **参考实现**：借鉴 PiliPlus 的成熟方案

## 测试验证

修复后应该验证：
1. 进入页面时UI正确显示默认排序的选中状态
2. 点击不同排序选项时，UI选中状态正确更新
3. 内容按照选中的排序方式正确显示
4. 多次切换排序都能正常工作
5. 加载更多时不影响当前排序状态
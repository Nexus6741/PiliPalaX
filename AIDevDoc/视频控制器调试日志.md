# 视频控制器调试日志

## 已添加的调试日志

在 `lib/pages/video/controller.dart` 的 `onInit` 方法中添加了详细的调试日志。

## 运行测试

1. 重新运行应用
2. 进入音乐推荐视频列表
3. 点击任意视频卡片
4. 查看控制台输出

## 预期日志输出

### 卡片点击日志
```
=== MusicVideoCardH 点击事件 ===
videoItem.bvid: BV1kkmgBnEir
videoItem.cid: 34662449997
videoItem.aid: 115700211392273
videoItem.title: 第一突击手的速度！
处理后 bvid: BV1kkmgBnEir
处理后 cid: 34662449997
准备跳转到: /video?bvid=BV1kkmgBnEir&cid=34662449997
arguments: {videoItem: Instance of 'BgmRecommend', pic: http://..., heroTag: music_video_BV1kkmgBnEir}
✅ Get.toNamed 调用完成
```

### 视频控制器日志
```
=== VideoDetailController.onInit ===
Get.parameters: {bvid: BV1kkmgBnEir, cid: 34662449997}
Get.arguments: {videoItem: Instance of 'BgmRecommend', pic: http://..., heroTag: music_video_BV1kkmgBnEir}
VideoDetailController args: {videoItem: Instance of 'BgmRecommend', pic: http://..., heroTag: music_video_BV1kkmgBnEir}
argMap keys: [videoItem, pic, heroTag]
videoItem type: BgmRecommend
videoItem: Instance of 'BgmRecommend'
```

## 可能的问题

### 问题1: BgmRecommend对象字段访问失败
如果看到：
```
获取videoItem.pic失败: NoSuchMethodError: ...
从videoItem.cover获取封面: http://...
```

说明BgmRecommend对象没有`pic`字段，但有`cover`字段。这是预期的，代码已经添加了fallback逻辑。

### 问题2: 视频控制器初始化失败
如果没有看到"=== VideoDetailController.onInit ==="日志，说明：
- 路由跳转失败
- 视频页面没有被创建
- 控制器没有被初始化

### 问题3: Get.parameters为空
如果看到：
```
Get.parameters: {}
```

说明查询字符串参数没有被正确解析。这可能是GetX路由配置问题。

## 解决方案

### 方案1: 创建videoItem适配器
如果BgmRecommend对象的字段不兼容，创建一个适配器：

```dart
// 在music_video_card_h.dart中
Map<String, dynamic> _convertToVideoItem(BgmRecommend item) {
  return {
    'pic': item.cover,
    'title': item.title,
    'bvid': item.bvid,
    'cid': item.cid,
    'aid': item.aid,
    'owner': {
      'name': item.upNickName,
      'mid': item.mid,
    },
    'stat': {
      'view': item.play,
      'danmu': item.danmu,
    },
    'duration': item.duration,
  };
}

// 在onTap中使用
Get.toNamed(
  '/video?bvid=$bvid&cid=$cid',
  arguments: {
    'videoItem': _convertToVideoItem(videoItem),
    'pic': videoItem.cover,
    'heroTag': 'music_video_${videoItem.bvid}',
  },
);
```

### 方案2: 修改BgmRecommend模型
给BgmRecommend添加getter来兼容视频控制器：

```dart
class BgmRecommend {
  // ... 现有字段 ...
  
  // 添加兼容性getter
  String? get pic => cover;
  
  Map<String, dynamic> get owner => {
    'name': upNickName,
    'mid': mid,
  };
  
  Map<String, dynamic> get stat => {
    'view': play,
    'danmu': danmu,
  };
}
```

### 方案3: 修改视频控制器
让视频控制器更灵活地处理不同类型的videoItem（已在代码中实现）。

## 下一步

1. 运行应用并点击视频
2. 复制完整的控制台日志
3. 根据日志判断具体问题
4. 选择合适的解决方案

## 关键检查点

- [ ] Get.parameters是否包含bvid和cid
- [ ] Get.arguments是否包含videoItem、pic、heroTag
- [ ] videoItem的类型是否为BgmRecommend
- [ ] 是否成功获取封面图片（从pic或cover字段）
- [ ] 最终videoItem是否包含pic字段

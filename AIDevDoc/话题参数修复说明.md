# 话题参数修复说明

## 问题描述
发布动态时选择了话题，但发送出去后却不带话题。

## 问题原因
在 `lib/pages/dynamics_create/controller.dart` 中定义了一个本地的 `TopicItem` 类：

```dart
class TopicItem {
  final int id;
  final String name;

  TopicItem({required this.id, required this.name});
}
```

但是在 `lib/http/dynamics.dart` 中导入的是 `lib/models/dynamics/topic_item.dart` 中的 `TopicItem` 类。

这导致类型不匹配，虽然编译通过（因为使用了 `dynamic` 类型），但在运行时访问 `topic.id` 和 `topic.name` 时可能出现问题。

## 修复方案

### 1. 删除本地 TopicItem 定义
在 `lib/pages/dynamics_create/controller.dart` 中删除了本地的 `TopicItem` 类定义。

### 2. 导入正确的 TopicItem 模型
添加导入语句：
```dart
import '../../models/dynamics/topic_item.dart';
```

### 3. 修改 createDynamic 方法签名
在 `lib/http/dynamics.dart` 中，将 `topic` 参数类型从 `dynamic` 改为 `TopicItem?`：

```dart
static Future createDynamic({
  required String content,
  String? title,
  List<String>? images,
  TopicItem? topic,  // 从 dynamic 改为 TopicItem?
  bool isPrivate = false,
  bool allowReply = true,
  DateTime? scheduledTime,
}) async {
```

## 话题参数的正确格式

根据 bilibili API 文档和 PiliPlus 的实现，话题参数应该放在 `dyn_req` 对象内部：

```dart
if (topic != null) {
  dynReq['topic'] = {
    'id': topic.id,           // 话题ID
    'name': topic.name,       // 话题名称
    'from_source': 'dyn.web.list',  // 来源
    'from_topic_id': 0,       // 来源话题ID
  };
}
```

## 验证修复

修复后，当用户选择话题并发布动态时：
1. `selectedTopic.value` 是一个 `TopicItem` 对象
2. 传递给 `DynamicsHttp.createDynamic` 时类型正确
3. 在构建请求数据时，正确访问 `topic.id` 和 `topic.name`
4. 发送到服务器的数据包含正确的话题信息

## 相关文件
- `lib/pages/dynamics_create/controller.dart` - 修复了 TopicItem 导入
- `lib/http/dynamics.dart` - 修复了参数类型
- `lib/models/dynamics/topic_item.dart` - 标准的 TopicItem 模型定义

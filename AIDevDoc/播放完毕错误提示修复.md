# 播放完毕错误提示修复说明

## 问题描述
播放视频完毕后，会出现"视频不存在或已被删除"的错误提示。

## 问题原因
在番剧的 `nextPlay()` 方法中存在逻辑错误：

```dart
// 原代码（错误）
int nextIndex = currentIndex + 1;
if (nextIndex == episodes.length - 1) {  // ❌ 错误：应该是 >= episodes.length
  // 处理列表循环等逻辑
}
// 继续执行，尝试访问 episodes[nextIndex]
```

### 问题分析
1. 当播放到最后一集时，`currentIndex = episodes.length - 1`
2. 计算 `nextIndex = currentIndex + 1 = episodes.length`
3. 条件判断 `nextIndex == episodes.length - 1` 为 `false`（因为 nextIndex 已经等于 episodes.length）
4. 跳过了列表循环的处理逻辑
5. 继续执行，尝试访问 `episodes[nextIndex]`，但 nextIndex 已经越界
6. 导致获取到错误的 cid，调用 API 时返回 -404 错误
7. 触发 `queryVideoUrl()` 中的错误处理，显示"视频不存在或已被删除"

## 修复方案

### 修改文件
`lib/pages/video/introduction/bangumi/controller.dart`

### 修复内容
```dart
// 修复后的代码
int nextIndex = currentIndex + 1;

// 🔥 修复：检查是否已经是最后一集
if (nextIndex >= episodes.length) {  // ✅ 正确：使用 >= 判断
  // 已经是最后一集
  if (playRepeat == PlayRepeat.listCycle) {
    // 列表循环，回到第一集
    nextIndex = 0;
  } else if (playRepeat == PlayRepeat.autoPlayRelated) {
    // 自动连播相关视频
    return playRelated();
  } else {
    // 顺序播放或暂停，停止播放
    return false;
  }
}

// 只有在 nextIndex 有效时才继续执行
int cid = episodes[nextIndex].cid!;
String bvid = episodes[nextIndex].bvid!;
int aid = episodes[nextIndex].aid!;
int? epid = episodes[nextIndex].id;
changeSeasonOrbangu(bvid, cid, aid, epid: epid);
return true;
```

### 额外改进
1. 添加了 `currentIndex == -1` 的检查，防止找不到当前集数时出错
2. 添加了 `episodes` 为空时的提前返回
3. 改进了代码注释，使逻辑更清晰

## 测试建议
1. 播放番剧到最后一集，等待播放完毕
2. 验证不再出现"视频不存在或已被删除"的错误提示
3. 测试不同的播放模式：
   - 顺序播放：最后一集播放完毕应该停止
   - 列表循环：最后一集播放完毕应该跳转到第一集
   - 自动连播：最后一集播放完毕应该播放相关视频（如果有）

## 影响范围
- 仅影响番剧/影视类视频的播放完毕逻辑
- 普通视频的逻辑已经是正确的，无需修改
- 不影响其他功能

## 修复状态
✅ 已完成

# 长按保存图片功能修复完成

## 修复内容

已为所有页面的卡片和头像统一添加长按直接保存图片功能，移除了原有的弹出对话框逻辑。

### 1. 视频卡片组件 (通用)
- **文件**: `lib/common/widgets/video_card_v.dart` 和 `lib/common/widgets/video_card_h.dart`
- **修改**: 为 `VideoCardV` 和 `VideoCardH` 添加默认长按保存功能
- **功能**: 长按视频封面直接保存图片，保存类型为 `cover`
- **影响页面**: 
  - 推荐页 (rcmd)
  - 热门页 (hot)
  - 分区页 (rank/zone)
  - 搜索结果页 (search_result)
  - 相关视频页 (video/related)
  - 稍后再看页 (later)
  - 收藏页 (fav_detail)
  - 个人主页投稿 (member_archive)
  - 等所有使用这两个组件的页面

### 2. 直播页 (Live)
- **文件**: `lib/common/widgets/live_card.dart`
- **修改**: 为 `LiveCard` 组件添加 `GestureDetector` 和 `onLongPress` 回调
- **功能**: 长按直播封面可保存图片，保存类型为 `live`

### 3. 直播页列表项
- **文件**: `lib/pages/live/widgets/live_item.dart`
- **修改**: 为 `LiveCardV` 组件的 `onLongPress` 添加默认保存功能
- **功能**: 如果没有自定义 `longPress` 回调，则默认保存直播封面

### 4. 番剧页 (Bangumi)
- **文件**: `lib/pages/bangumi/widgets/bangumi_card_v.dart`
- **修改**: 为 `BangumiCardV` 组件添加 `onLongPress` 回调
- **功能**: 长按番剧封面可保存图片，保存类型为 `bangumi`

### 5. 影视页 (PGC)
- **文件**: `lib/pages/pgc/widgets/pgc_card_v.dart`
- **修改**: 为 `PgcCardV` 组件添加 `onLongPress` 回调
- **功能**: 长按影视封面可保存图片，保存类型为 `pgc`

### 6. 个人资料页头像
- **文件**: `lib/pages/member/widgets/profile.dart`
- **修改**: 为头像的 `Hero` 组件外层添加 `GestureDetector`
- **功能**: 长按用户头像可保存图片，保存类型为 `avatar`

## 技术实现

所有修改都使用了统一的 `DownloadUtils.downloadImg()` 方法：

```dart
DownloadUtils.downloadImg(context, imageUrl, imgType: 'type');
```

### 保存类型说明
- `cover`: 视频封面（推荐页、热门页、分区页等）
- `live`: 直播封面
- `bangumi`: 番剧封面
- `pgc`: 影视封面
- `avatar`: 用户头像

## 移除的功能

已移除以下页面的弹出对话框逻辑：
1. **推荐页** (`lib/pages/rcmd/view.dart`) - 移除 `_createPopupDialog` 和 `_removePopupDialog` 相关调用
2. **热门页** (`lib/pages/hot/view.dart`) - 移除 `_createPopupDialog` 和 `_removePopupDialog` 相关调用
3. **分区页** (`lib/pages/rank/zone/view.dart`) - 移除 `_createPopupDialog` 和 `_removePopupDialog` 相关调用
4. **相关视频页** (`lib/pages/video/related/view.dart`) - 移除 `_createPopupDialog` 和 `_removePopupDialog` 相关调用

现在所有页面的长按行为统一为：**直接保存图片**

## 测试建议

1. **推荐页**: 长按视频卡片封面，应直接保存图片
2. **热门页**: 长按视频卡片封面，应直接保存图片
3. **分区页**: 长按视频卡片封面，应直接保存图片
4. **相关视频页**: 长按视频卡片封面，应直接保存图片
5. **直播页**: 长按直播卡片封面，应直接保存图片
6. **番剧页**: 长按番剧卡片封面，应直接保存图片
7. **影视页**: 长按影视卡片封面，应直接保存图片
8. **个人资料页**: 长按用户头像，应直接保存图片
9. **搜索结果页**: 长按视频卡片封面，应直接保存图片

## 注意事项

- 所有图片保存到 `Pictures/PiliPalaX` 文件夹
- 图片文件名格式: `{type}_{timestamp}.jpg`
- 保存前会下载原图，保存时显示加载提示
- 需要相应的存储权限（Android）
- 长按行为现在统一为直接保存，不再弹出对话框

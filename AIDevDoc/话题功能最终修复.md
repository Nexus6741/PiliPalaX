# 话题功能最终修复

## 问题描述

点击话题页的点赞和收藏按钮时报错，API 调用失败。

## 问题原因

API 端点和参数与实际网页版请求不匹配。

## 解决方案

### 1. 点赞 API 修复

**修改文件**：`lib/http/user.dart` - `likeTopic()` 方法

**修改前**：
```dart
// 使用错误的参数格式
```

**修改后**：
```dart
static Future<dynamic> likeTopic(String topicId, bool isLike, int upMid) async {
  try {
    var res = await Request().post(
      Api.likeTopic,
      data: {
        'csrf': await Request.getCsrf(),
        'action': isLike ? 'cancel_like' : 'like',  // 使用 action 参数
        'up_mid': upMid,                             // 添加 up_mid
        'topic_id': topicId,
        'business': 'topic',                         // 添加 business
      },
    );
    if (res.data['code'] == 0) {
      return {'status': true};
    } else {
      return {'status': false, 'msg': res.data['message']};
    }
  } catch (err) {
    return {'status': false, 'msg': err.toString()};
  }
}
```

**关键变更**：
- 使用 `action` 参数：`like` 或 `cancel_like`
- 添加 `up_mid` 参数（话题创建者 UID）
- 添加 `business: 'topic'` 参数
- 使用 `data` 而非 `queryParameters`

### 2. 收藏 API 修复

**修改文件**：`lib/http/user.dart` - `addFavTopic()` 和 `delFavTopic()` 方法

**修改前**：
```dart
// 使用相同的端点
```

**修改后**：
```dart
// 收藏话题
static Future<dynamic> addFavTopic(String topicId) async {
  try {
    var res = await Request().post(
      Api.addFavTopic,  // 独立端点：/x/topic/fav/sub/add
      data: {
        'csrf': await Request.getCsrf(),
        'topic_id': topicId,
      },
    );
    if (res.data['code'] == 0) {
      return {'status': true};
    } else {
      return {'status': false, 'msg': res.data['message']};
    }
  } catch (err) {
    return {'status': false, 'msg': err.toString()};
  }
}

// 取消收藏话题
static Future<dynamic> delFavTopic(String topicId) async {
  try {
    var res = await Request().post(
      Api.delFavTopic,  // 独立端点：/x/topic/fav/sub/cancel
      data: {
        'csrf': await Request.getCsrf(),
        'topic_id': topicId,
      },
    );
    if (res.data['code'] == 0) {
      return {'status': true};
    } else {
      return {'status': false, 'msg': res.data['message']};
    }
  } catch (err) {
    return {'status': false, 'msg': err.toString()};
  }
}
```

**关键变更**：
- 使用独立的端点：`addFavTopic` 和 `delFavTopic`
- 使用 `data` 而非 `queryParameters`

### 3. API 端点定义

**修改文件**：`lib/http/api.dart`

```dart
// 收藏话题
static const String addFavTopic = '${HttpString.appBaseUrl}/x/topic/fav/sub/add';

// 取消收藏话题
static const String delFavTopic = '${HttpString.appBaseUrl}/x/topic/fav/sub/cancel';

// 点赞话题
static const String likeTopic = '${HttpString.appBaseUrl}/x/topic/like';
```

**关键变更**：
- 使用 `${HttpString.appBaseUrl}` (https://app.bilibili.com)
- 收藏和取消收藏使用不同的端点

### 4. Controller 调用

**修改文件**：`lib/pages/dynamics_topic/controller.dart`

```dart
Future<void> onLike() async {
  if (!isLogin) {
    SmartDialog.showToast('账号未登录');
    return;
  }
  bool currentIsLike = isLike.value ?? false;
  int upMid = topDetails.value?.topicCreator?.uid ?? 0;  // 获取创建者 UID
  var res = await UserHttp.likeTopic(topicId, currentIsLike, upMid);
  if (res['status']) {
    if (topDetails.value?.topicItem != null) {
      if (currentIsLike) {
        topDetails.value!.topicItem!.like -= 1;
      } else {
        topDetails.value!.topicItem!.like += 1;
      }
    }
    isLike.value = !currentIsLike;
    SmartDialog.showToast(currentIsLike ? '已取消点赞' : '点赞成功');
  } else {
    SmartDialog.showToast(res['msg'] ?? '操作失败');
  }
}
```

**关键变更**：
- 从 `topDetails.value?.topicCreator?.uid` 获取 `upMid`
- 传递给 `likeTopic()` 方法

## 实际网页版请求参考

### 点赞
```
POST https://app.bilibili.com/x/topic/like
Content-Type: application/x-www-form-urlencoded

csrf=xxx&action=like&up_mid=38248199&topic_id=1326142&business=topic
```

### 取消点赞
```
POST https://app.bilibili.com/x/topic/like
Content-Type: application/x-www-form-urlencoded

csrf=xxx&action=cancel_like&up_mid=38248199&topic_id=1326142&business=topic
```

### 收藏
```
POST https://app.bilibili.com/x/topic/fav/sub/add
Content-Type: application/x-www-form-urlencoded

csrf=xxx&topic_id=1326142
```

### 取消收藏
```
POST https://app.bilibili.com/x/topic/fav/sub/cancel
Content-Type: application/x-www-form-urlencoded

csrf=xxx&topic_id=1326142
```

## 测试验证

1. ✅ 点击点赞按钮 - 应该成功点赞
2. ✅ 再次点击点赞按钮 - 应该取消点赞
3. ✅ 点击收藏按钮 - 应该成功收藏
4. ✅ 再次点击收藏按钮 - 应该取消收藏
5. ✅ 点赞数和收藏数应该正确更新
6. ✅ 按钮状态应该正确切换

## 总结

通过分析实际网页版的网络请求，修复了话题页的点赞和收藏功能：

1. **点赞 API**：使用 `action` 参数区分点赞和取消点赞，添加 `up_mid` 和 `business` 参数
2. **收藏 API**：使用独立的添加和取消端点
3. **请求方式**：POST 请求使用 `data` 参数
4. **API 基础 URL**：使用 `app.bilibili.com` 而非 `api.bilibili.com`

所有修改都基于实际的网页版 API 请求，确保与 B 站服务器的兼容性。

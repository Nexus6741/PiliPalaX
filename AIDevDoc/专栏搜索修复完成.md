# 专栏搜索结果打开修复完成

## 问题描述
1. 搜索后得到的专栏结果，打开后只显示标题，内容和评论都无法加载
2. 浏览专栏后没有上传到历史记录

## 问题原因
1. **内容加载问题**：PiliPalaX项目使用HTML爬取方式获取专栏内容（`lib/http/html.dart`中的`reqReadHtml`方法），通过解析网页DOM来提取内容。这种方式不稳定，容易因为：
   - 网页结构变化导致解析失败
   - 反爬虫机制拦截
   - SSR渲染问题

2. **历史记录问题**：缺少专栏浏览历史记录上报功能

## 解决方案
参考PiliPlus项目的实现：
1. 改用B站官方API `/x/article/view` 来获取专栏内容
2. 添加历史记录上报功能，使用 `/x/v2/history/report` API

## 修改内容

### 1. 添加API定义 (`lib/http/api.dart`)
```dart
/// 专栏内容
/// https://api.bilibili.com/x/article/view?id=xxx
static const String articleView = '/x/article/view';

/// 上报观看历史记录
/// https://api.bilibili.com/x/v2/history/report
static const String historyReport = '/x/v2/history/report';
```

### 2. 重写专栏内容获取方法 (`lib/http/html.dart`)
将原来的HTML爬取方式改为API调用方式：

**修改前：**
- 使用`Request().get()`爬取网页HTML
- 使用`html/parser.dart`解析DOM
- 通过CSS选择器提取内容

**修改后：**
- 调用`Api.articleView` API接口
- 直接从JSON响应中获取结构化数据
- 支持HTML格式(type=0)和JSON格式(type=3)的专栏内容

### 3. 添加历史记录上报方法 (`lib/http/video.dart`)
```dart
// 上报观看历史记录
// type: 3=视频, 4=番剧, 5=专栏
static Future historyReport({
  required int aid,
  int? type,
}) async {
  try {
    await Request().post(
      Api.historyReport,
      data: {
        'aid': aid,
        if (type != null) 'type': type,
        'csrf': await Request.getCsrf(),
      },
    );
  } catch (e) {
    print('historyReport error: $e');
  }
}
```

### 4. 在专栏控制器中调用历史记录上报 (`lib/pages/html/controller.dart`)
```dart
// 上报历史记录
void _reportHistory() async {
  try {
    // 检查是否登录和是否暂停历史记录
    var userInfo = GStorage.userInfo.get('userInfoCache');
    bool historyPause = setting.get(SettingBoxKey.enableSaveLastData, defaultValue: false);
    
    if (userInfo != null && userInfo.mid != null && !historyPause) {
      // type: 5 表示专栏
      await VideoHttp.historyReport(aid: oid.value, type: 5);
    }
  } catch (e) {
    print('_reportHistory error: $e');
  }
}
```

### 5. 主要改进
- ✅ 使用官方API，稳定可靠
- ✅ 支持多种专栏格式（HTML和JSON）
- ✅ 更好的错误处理
- ✅ 获取完整的作者信息
- ✅ 正确提取评论ID
- ✅ 自动上报浏览历史记录
- ✅ 尊重用户的历史记录暂停设置

## API说明

### 专栏内容API
**请求：**
```
GET https://api.bilibili.com/x/article/view?id={cvId}
```

**响应字段：**
- `author`: 作者信息（头像、昵称等）
- `content`: 专栏内容（HTML或JSON字符串）
- `type`: 内容类型（0=HTML, 3=JSON）
- `title`: 标题
- `stats`: 统计数据（浏览、点赞、收藏等）

### 历史记录上报API
**请求：**
```
POST https://api.bilibili.com/x/v2/history/report
Content-Type: application/x-www-form-urlencoded

aid={专栏ID}&type=5&csrf={csrf_token}
```

**参数说明：**
- `aid`: 专栏ID（数字部分，如cv123456中的123456）
- `type`: 内容类型（5表示专栏）
- `csrf`: CSRF Token（从cookie中获取）

## 测试建议
1. 搜索专栏关键词
2. 点击搜索结果中的专栏条目
3. 验证内容是否正常显示
4. 检查评论区是否可以加载
5. 测试不同类型的专栏（旧版HTML格式和新版JSON格式）
6. 查看历史记录页面，确认专栏浏览记录已上传
7. 测试历史记录暂停功能是否生效

## 参考
- PiliPlus项目实现：
  - `PiliPlus/lib/http/dynamics.dart` - `articleView`方法
  - `PiliPlus/lib/pages/article/controller.dart` - 历史记录上报
- B站API文档：
  - `bilibili-API-collect/docs/article/view.md` - 专栏内容API
  - `bilibili-API-collect/docs/video/report.md` - 历史记录上报API

# 选集切换进度问题修复

## 问题描述
当观看选集视频时，切换到下一集或手动选择某一集时，新视频的播放进度会继承上一集的进度位置，而不是从头开始播放。

例如：
- 第1集播放到 33% 位置
- 切换到第2集
- 第2集也从 33% 位置开始播放（错误❌）
- 期望：第2集应该从头播放（正确✅）

## 问题原因
在切换选集时，虽然代码中有 `videoDetailCtr.defaultST = null;` 来清空播放位置，但这个设计存在问题：

1. **设置为 `null` 的意图**：让 `queryVideoUrl()` 方法使用新选集的历史记录 `data.lastPlayTime`
2. **实际问题**：
   - `data.lastPlayTime` 可能返回的是旧的或错误的值
   - 或者在某些情况下，`defaultST` 在被设置为 `null` 后又被其他逻辑修改
   - 导致新选集使用了错误的播放位置

## 解决方案

### 修改思路
添加 `useHistory` 参数来区分不同的场景：
- **手动切换选集**：`useHistory = false`（默认），从头播放
- **自动跳转到历史记录**：`useHistory = true`，使用历史记录位置

### 修改文件

#### 1. `lib/pages/video/introduction/bangumi/controller.dart`
在 `changeSeasonOrbangu` 方法中添加 `useHistory` 参数：

```dart
Future changeSeasonOrbangu(bvid, cid, aid, {int? epid, bool useHistory = false}) async {
  // ...
  
  // 🔥 修复：根据 useHistory 参数决定播放位置
  // useHistory = true: 使用历史记录位置（首次打开或自动跳转）
  // useHistory = false: 从头播放（手动切换选集）
  if (useHistory) {
    videoDetailCtr.defaultST = null; // 使用历史记录
  } else {
    videoDetailCtr.defaultST = Duration.zero; // 从头播放
  }
  
  videoDetailCtr.queryVideoUrl();
}
```

**修改位置**：第 306-328 行

#### 2. `lib/pages/video/introduction/detail/controller.dart`
在 `changeSeasonOrbangu` 方法中添加 `useHistory` 参数：

```dart
Future changeSeasonOrbangu(bvid, cid, aid, {int? epid, bool useHistory = false}) async {
  // ...
  
  // 🔥 修复：根据 useHistory 参数决定播放位置
  // useHistory = true: 使用历史记录位置（首次打开或自动跳转）
  // useHistory = false: 从头播放（手动切换选集）
  if (useHistory) {
    videoDetailCtr.defaultST = null; // 使用历史记录
  } else {
    videoDetailCtr.defaultST = Duration.zero; // 从头播放
  }
  
  videoDetailCtr.queryVideoUrl();
}
```

**修改位置**：第 493-515 行

#### 3. `lib/pages/video/controller.dart`
在 `checkContinuePlay` 方法中，调用 `changeSeasonOrbangu` 时传递 `useHistory: true`：

```dart
// 自动跳转到上次观看的集数，并使用历史记录位置
bangumiIntroController.changeSeasonOrbangu(
  episode.bvid,
  episode.cid,
  episode.aid,
  epid: episode.id,
  useHistory: true, // 使用历史记录位置
);
```

**修改位置**：第 670、700、732、756 行（共4处）

## 工作原理

### 场景1：手动切换选集（默认行为）
1. 用户手动点击切换选集
2. 调用 `changeSeasonOrbangu(bvid, cid, aid)` - 不传递 `useHistory` 参数
3. `useHistory` 默认为 `false`
4. 设置 `defaultST = Duration.zero` ✅
5. `queryVideoUrl()` 检测到 `defaultST != null`
6. **跳过**设置 `defaultST`，保持 `Duration.zero`
7. `playerInit()` 使用 `Duration.zero` 从头开始播放 ✅

### 场景2：自动跳转到历史记录
1. 首次打开视频，检测到有历史记录
2. `checkContinuePlay()` 调用 `changeSeasonOrbangu(..., useHistory: true)` ✅
3. `useHistory` 为 `true`
4. 设置 `defaultST = null` ✅
5. `queryVideoUrl()` 检测到 `defaultST == null`
6. 设置 `defaultST = Duration(milliseconds: data.lastPlayTime!)` ✅
7. `playerInit()` 使用历史记录位置开始播放 ✅

### 场景3：自动播放下一集
1. 当前集播放完成，自动播放下一集
2. `nextPlay()` 调用 `changeSeasonOrbangu(bvid, cid, aid)` - 不传递 `useHistory`
3. `useHistory` 默认为 `false`
4. 从头播放下一集 ✅

## 测试建议

### 测试场景
1. **手动切换选集（从头播放）**
   - 播放第1P到中间位置（如 30%）
   - 点击切换到第2P
   - ✅ 验证：第2P应该从头播放

2. **首次打开有历史记录的视频（使用历史记录）**
   - 之前观看过某个视频，在第2P的 50% 位置退出
   - 重新打开这个视频
   - ✅ 验证：应该自动跳转到第2P，并从 50% 位置继续播放

3. **番剧选集切换（从头播放）**
   - 播放第1集到中间位置（如 50%）
   - 点击切换到第2集
   - ✅ 验证：第2集应该从头播放

4. **番剧历史记录跳转（使用历史记录）**
   - 之前观看过某个番剧，在第3集的 40% 位置退出
   - 重新打开这个番剧
   - ✅ 验证：应该自动跳转到第3集，并从 40% 位置继续播放

5. **自动播放下一集（从头播放）**
   - 播放第1集到结束
   - 自动播放第2集
   - ✅ 验证：第2集应该从头播放

6. **快速连续切换（从头播放）**
   - 快速切换多个选集
   - ✅ 验证：每个选集都从头播放

## 注意事项

### 历史记录功能
这个修复实现了智能的播放位置管理：
- **手动切换选集**：总是从头播放 ✅
- **首次打开视频**：如果有历史记录，自动跳转到历史记录的选集和位置 ✅
- **自动播放下一集**：从头播放 ✅

这样既避免了错误的进度继承，又保留了历史记录功能。

### 相关功能
- **继续播放功能**：不受影响，因为它使用的是 `resumePlay` 参数
- **画质切换**：不受影响，因为 `updatePlayer()` 会保存当前播放位置
- **自动跳转到上次观看的分P**：不受影响，因为 `checkContinuePlay()` 是独立的功能

## 相关文件
- `lib/pages/video/controller.dart` - 视频详情控制器
- `lib/pages/video/introduction/detail/controller.dart` - 普通视频简介控制器
- `lib/pages/video/introduction/bangumi/controller.dart` - 番剧简介控制器
- `lib/plugin/pl_player/controller.dart` - 播放器控制器

## 修复日期
2025-12-14

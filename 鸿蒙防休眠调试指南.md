# 鸿蒙防休眠功能调试指南

## 问题现象
播放视频时设备仍然会自动息屏

## 调试步骤

### 1. 检查日志输出

重新编译并运行应用，播放视频时查看日志输出，应该看到以下信息：

```
WakelockManager: 开始启用防休眠...
WakelockManager: 当前平台 - ohos
WakelockManager: 调用 WakelockOhos.enable()
WakelockOhos: 调用原生 enable 方法，timeout=-1
WakelockOhos: 原生 enable 返回结果: true
WakelockManager: ✅ 鸿蒙防休眠已启用成功
```

### 2. 检查插件是否正确加载

如果看到以下错误：
```
MissingPluginException(No implementation found for method enable on channel wakelock_ohos)
```

说明插件没有正确加载，需要：

1. **检查 pubspec.yaml**
   ```yaml
   dependencies:
     wakelock_ohos:
       path: ./packages/wakelock_ohos
   ```

2. **运行 flutter pub get**
   ```bash
   flutter pub get
   ```

3. **检查鸿蒙项目配置**
   - 确认 `packages/wakelock_ohos/ohos/` 目录存在
   - 确认 `packages/wakelock_ohos/ohos/hvigorfile.ts` 存在
   - 确认 `packages/wakelock_ohos/ohos/oh-package.json5` 存在

4. **清理并重新编译**
   ```bash
   flutter clean
   # 重新编译鸿蒙应用
   ```

### 3. 检查权限配置

确认 `ohos/entry/src/main/module.json5` 中包含：

```json5
requestPermissions: [
  { name: "ohos.permission.RUNNING_LOCK" }
]
```

### 4. 检查原生代码

查看 `packages/wakelock_ohos/ohos/src/main/ets/components/plugin/WakelockOhosPlugin.ets`：

- 确认文件存在
- 确认导入了 `runningLock`
- 确认实现了 `enable` 和 `disable` 方法

### 5. 测试原生 API

在鸿蒙原生代码中添加测试日志，确认 `runningLock` API 可用：

```typescript
// 在 enableWakelock 方法中
console.info(TAG, 'Creating runningLock...');
const supported = runningLock.isSupported(runningLock.RunningLockType.PROXIMITY_SCREEN_CONTROL);
console.info(TAG, `RunningLock supported: ${supported}`);
```

### 6. 检查播放器集成

确认 `lib/plugin/pl_player/controller.dart` 中：

**play 方法：**
```dart
Future<void> play({bool repeat = false, bool hideControls = true}) async {
  // ... 其他代码
  await _videoPlayerController?.play();
  playerStatus.status.value = PlayerStatus.playing;
  
  // 启用防休眠
  WakelockManager.instance.enable();  // ← 确认这行存在
}
```

**pause 方法：**
```dart
Future<void> pause({bool notify = true, bool isInterrupt = false}) async {
  await _videoPlayerController?.pause();
  playerStatus.status.value = PlayerStatus.paused;
  
  // ... 其他代码
  
  // 禁用防休眠
  WakelockManager.instance.disable();  // ← 确认这行存在
}
```

## 常见问题

### Q1: 日志显示"当前平台 - android"而不是"ohos"
**原因：** 可能在 Android 模拟器或设备上运行
**解决：** 确保在鸿蒙设备或模拟器上运行

### Q2: 日志显示"❌ 鸿蒙防休眠启用失败"
**原因：** 原生 API 调用失败
**检查：**
1. 权限是否正确配置
2. 原生代码是否有错误
3. 查看原生日志输出

### Q3: 没有看到任何 WakelockManager 日志
**原因：** 播放器没有调用防休眠功能
**检查：**
1. 确认导入了 `wakelock_manager.dart`
2. 确认在 play/pause 方法中添加了调用
3. 确认播放器的 play 方法被正确执行

### Q4: 插件编译错误
**解决：**
```bash
# 清理缓存
flutter clean

# 删除 ohos/entry/build 目录
rm -rf ohos/entry/build

# 重新获取依赖
flutter pub get

# 重新编译
```

## 手动测试

在视频页面添加测试按钮：

```dart
ElevatedButton(
  onPressed: () async {
    print('=== 开始测试防休眠 ===');
    
    // 测试启用
    await WakelockManager.instance.enable();
    await Future.delayed(Duration(seconds: 2));
    
    // 检查状态
    bool enabled = await WakelockManager.instance.isEnabled();
    print('防休眠状态: $enabled');
    
    // 测试禁用
    await WakelockManager.instance.disable();
    
    print('=== 测试完成 ===');
  },
  child: Text('测试防休眠'),
)
```

## 预期行为

1. **播放视频时：**
   - 日志显示"✅ 鸿蒙防休眠已启用成功"
   - 屏幕保持常亮，不会自动息屏

2. **暂停视频时：**
   - 日志显示"✅ 鸿蒙防休眠已禁用"
   - 屏幕可以正常息屏

3. **退出播放器时：**
   - 日志显示"✅ 鸿蒙防休眠已禁用"
   - 锁已释放

## 下一步

如果以上步骤都检查无误但仍然息屏，可能需要：

1. 尝试使用不同的 RunningLockType（如 BACKGROUND）
2. 检查鸿蒙系统版本是否支持该 API
3. 查看鸿蒙系统的电源管理设置
4. 检查是否有其他应用或系统设置干扰

## 联系支持

如果问题仍未解决，请提供：
1. 完整的日志输出
2. 鸿蒙系统版本
3. 设备型号
4. 复现步骤

# 防休眠功能验证清单

## 当前实现状态

✅ **播放时启用防休眠**
- 位置: `lib/plugin/pl_player/controller.dart` - `play()` 方法
- 代码: `WakelockManager.instance.enable();`

✅ **暂停时禁用防休眠**
- 位置: `lib/plugin/pl_player/controller.dart` - `pause()` 方法
- 代码: `WakelockManager.instance.disable();`

✅ **退出时释放防休眠**
- 位置: `lib/plugin/pl_player/controller.dart` - `dispose()` 方法
- 代码: `WakelockManager.instance.disable();`

✅ **关闭时释放防休眠**
- 位置: `lib/plugin/pl_player/controller.dart` - `videoPlayerClosed()` 方法
- 代码: `WakelockManager.instance.disable();`

## 测试步骤

### 1. 播放测试

**操作：**
1. 打开应用
2. 播放任意视频
3. 等待超过系统息屏时间（如 30 秒）

**预期日志：**
```
WakelockManager: 开始启用防休眠...
WakelockManager: 当前平台 - ohos
WakelockOhosPlugin: ✅ Screen keep on enabled successfully
WakelockManager: ✅ 鸿蒙防休眠已启用成功
```

**预期结果：**
- ✅ 屏幕保持常亮
- ✅ 不会自动息屏

### 2. 暂停测试

**操作：**
1. 播放视频
2. 点击暂停按钮
3. 等待系统息屏时间

**预期日志：**
```
WakelockManager: 开始禁用防休眠...
WakelockOhosPlugin: ✅ Screen keep on disabled successfully
WakelockManager: ✅ 鸿蒙防休眠已禁用
```

**预期结果：**
- ✅ 屏幕可以正常息屏
- ✅ 恢复系统默认行为

### 3. 继续播放测试

**操作：**
1. 暂停视频
2. 再次点击播放
3. 等待超过息屏时间

**预期日志：**
```
# 暂停时
WakelockManager: ✅ 鸿蒙防休眠已禁用

# 再次播放时
WakelockManager: 开始启用防休眠...
WakelockManager: ✅ 鸿蒙防休眠已启用成功
```

**预期结果：**
- ✅ 屏幕再次保持常亮

### 4. 退出测试

**操作：**
1. 播放视频
2. 退出播放页面（返回或关闭）

**预期日志：**
```
WakelockManager: 开始禁用防休眠...
WakelockManager: ✅ 鸿蒙防休眠已禁用
```

**预期结果：**
- ✅ 锁已释放
- ✅ 其他页面可以正常息屏

### 5. 快速切换测试

**操作：**
1. 快速点击播放/暂停按钮多次
2. 观察日志和屏幕行为

**预期结果：**
- ✅ 状态切换正常
- ✅ 没有错误日志
- ✅ 屏幕行为符合预期

## 代码位置总览

### 播放器控制器
`lib/plugin/pl_player/controller.dart`

```dart
// 播放时启用
Future<void> play({bool repeat = false, bool hideControls = true}) async {
  // ... 其他代码
  await _videoPlayerController?.play();
  playerStatus.status.value = PlayerStatus.playing;
  
  // 启用防休眠
  WakelockManager.instance.enable();  // ← 第 1098 行
}

// 暂停时禁用
Future<void> pause({bool notify = true, bool isInterrupt = false}) async {
  await _videoPlayerController?.pause();
  playerStatus.status.value = PlayerStatus.paused;
  
  // ... 其他代码
  
  // 禁用防休眠
  WakelockManager.instance.disable();  // ← 第 1117 行
}

// 销毁时释放
Future<void> dispose() async {
  pause();
  
  // 释放防休眠锁
  WakelockManager.instance.disable();  // ← 第 1808 行
  
  // ... 其他清理代码
}

// 关闭时释放
Future<void> videoPlayerClosed() async {
  // ... 其他代码
  
  // 释放防休眠锁
  WakelockManager.instance.disable();  // ← 第 1651 行
}
```

### 防休眠管理器
`lib/utils/wakelock_manager.dart`

```dart
class WakelockManager {
  static WakelockManager get instance => _instance;
  
  Future<void> enable() async {
    if (Platform.operatingSystem == 'ohos') {
      await WakelockOhos.enable(timeout: -1);
    } else {
      await WakelockPlus.enable();
    }
  }
  
  Future<void> disable() async {
    if (Platform.operatingSystem == 'ohos') {
      await WakelockOhos.disable();
    } else {
      await WakelockPlus.disable();
    }
  }
}
```

### 鸿蒙插件
`packages/wakelock_ohos/ohos/src/main/ets/components/plugin/WakelockOhosPlugin.ets`

```typescript
private async setKeepScreenOn(isKeepScreenOn: boolean): Promise<void> {
  const windowClass = await window.getLastWindow(getContext(this));
  await windowClass.setWindowKeepScreenOn(isKeepScreenOn);
}
```

## 常见问题

### Q1: 暂停后仍然不息屏

**可能原因：**
1. 日志显示禁用失败
2. 其他地方也在持有锁
3. 系统设置问题

**排查步骤：**
1. 查看日志，确认看到 "✅ 鸿蒙防休眠已禁用"
2. 检查是否有其他代码也在控制屏幕常亮
3. 检查系统电源管理设置

### Q2: 暂停后没有看到禁用日志

**可能原因：**
- pause 方法没有被调用

**排查步骤：**
1. 在 pause 方法开头添加日志
2. 确认暂停按钮是否正确触发 pause 方法

### Q3: 快速切换时出现异常

**可能原因：**
- 状态管理问题

**解决方案：**
- WakelockManager 已经有状态检查，应该不会有问题

## 验证完成标准

- ✅ 播放时屏幕保持常亮
- ✅ 暂停时屏幕可以息屏
- ✅ 继续播放时屏幕再次常亮
- ✅ 退出时锁已释放
- ✅ 快速切换无异常
- ✅ 日志输出正确

## 下一步

如果所有测试通过：
- ✅ 功能完全正常
- ✅ 可以正常使用

如果测试失败：
- 查看详细日志
- 参考本文档的排查步骤
- 检查代码实现

## 更新日期

2025-12-17

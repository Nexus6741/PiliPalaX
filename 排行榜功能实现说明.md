# 排行榜功能实现说明

## 功能概述
在热门tab右上角添加了排行榜入口按钮，点击后可以进入排行榜页面查看哔哩哔哩近期的视频排行。

## 实现内容

### 1. 创建排行榜页面
- **文件**: `lib/pages/rank/view.dart`
- **功能**: 排行榜主页面，包含多个分类tab
- **特点**: 
  - 使用TabBar实现分类切换
  - 支持横向滚动查看更多分类
  - 每个tab复用现有的ZonePage组件

### 2. 创建排行榜控制器
- **文件**: `lib/pages/rank/controller.dart`
- **功能**: 管理排行榜的tab配置
- **分类包括**:
  - 全站、动画、国创、音乐、舞蹈、游戏
  - 知识、科技、运动、汽车、生活、美食
  - 动物圈、鬼畜、时尚、娱乐、影视
  - 原创、新人

### 3. 修改热门页面
- **文件**: `lib/pages/hot/view.dart`
- **修改内容**: 在页面顶部添加排行榜入口按钮
- **样式**: 使用FilledButton.tonalIcon，带有排行榜图标

### 4. 添加路由配置
- **文件**: `lib/router/app_pages.dart`
- **路由**: `/rank`
- **页面**: RankPage

## API说明

### 视频排行榜API
- **接口**: `GET /x/web-interface/ranking/v2`
- **参数**:
  - `rid`: 分区ID（0为全站）
  - `type`: 排行榜类型（all/origin/rookie）
  - `web_location`: 333.934
  - `w_rid`: WBI签名（可选）
  - `wts`: Unix时间戳（可选）

### 现有实现
项目中已有的`VideoHttp.getRankVideoList(rid)`方法可以直接使用，该方法：
- 调用排行榜API获取数据
- 过滤黑名单用户
- 返回HotVideoItemModel列表

## 使用方式

1. 打开应用，进入热门tab
2. 点击右上角的"排行榜"按钮
3. 在排行榜页面选择感兴趣的分类
4. 查看该分类下的热门视频排行

## 技术细节

### 页面结构
```
RankPage (排行榜主页)
├── AppBar (标题栏)
│   └── TabBar (分类标签栏)
└── TabBarView (内容区域)
    └── ZonePage (各分类页面，复用现有组件)
```

### 数据流
```
用户点击分类 → ZonePage加载 → ZoneController.queryRankFeed() 
→ VideoHttp.getRankVideoList() → API请求 → 数据展示
```

## 注意事项

1. **复用现有组件**: 排行榜页面复用了现有的`ZonePage`组件，保持了UI一致性
2. **API兼容**: 使用了项目中已有的API方法，无需额外实现
3. **性能优化**: 使用`AutomaticKeepAliveClientMixin`保持页面状态
4. **用户体验**: 
   - Tab支持横向滚动
   - 保持Material Design风格
   - 入口按钮位置醒目但不突兀

## 后续优化建议

1. 可以添加排行榜类型切换（全部/原创/新人）
2. 可以添加时间范围筛选
3. 可以添加番剧、电影等PGC内容的排行榜（需要使用不同的API）
4. 可以添加排行榜数据缓存机制

## 参考文档

- [哔哩哔哩API文档 - 视频排行榜](bilibili-API-collect/docs/video_ranking/ranking.md)
- 网页端哔哩哔哩排行榜请求报文.md

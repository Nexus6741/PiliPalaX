# 评论楼中楼功能实现完成

## 实现的功能

参考 piliplus 的实现,为项目添加了以下评论楼中楼功能:

### 1. 按热度/时间排序 ❌ (API 不支持)
- B站的楼中楼 HTTP API (`/x/v2/reply/reply`) 不支持排序参数
- piliplus 通过 gRPC API 实现此功能,需要较大改动
- 当前项目暂不支持此功能

### 2. 查看对话 ✅
- 在二楼评论中添加"对话"按钮
- 点击可查看该评论的完整对话链
- 自动定位到父评论位置

**实现位置:**
- `lib/pages/video/reply/widgets/reply_item.dart` - 在 `buttonAction` 中添加对话按钮
- 判断条件: `replyLevel == '2' && parent != root && dialog > 0`

### 3. 定位评论并高亮 ✅
- 支持通过 `id` 参数跳转到指定评论
- 自动滚动到目标评论位置
- 使用渐变动画高亮显示目标评论(1秒后淡出)

**实现位置:**
- `lib/pages/video/reply_reply/controller.dart` - 添加 `targetRpid`、`highlightIndex`、`animationController`
- `lib/pages/video/reply_reply/view.dart` - 添加高亮动画效果
- 使用 `AnimatedBuilder` 和 `ColorTween` 实现渐变效果

### 4. 回复提示优化 ✅
- 在评论内容前显示"回复 @用户名:"
- 点击用户名可定位到父评论或跳转到用户主页
- 自动从 `atNameToMid` 中提取被回复用户信息

**实现位置:**
- `lib/pages/video/reply/widgets/reply_item.dart` - 在 `buildContent` 函数开始处添加回复提示

## 技术细节

### 排序功能
```dart
// 排序模式: 0=按时间, 1=按热度
RxInt sortMode = 1.obs;

void toggleSortMode() {
  sortMode.value = sortMode.value == 1 ? 0 : 1;
  queryReplyList(type: 'init');
}
```

### 定位功能
```dart
// 定位目标评论
void _locateTargetReply() {
  if (targetRpid == null) return;
  final index = replyList.indexWhere((item) => item.rpid == targetRpid);
  if (index != -1) {
    highlightIndex.value = index;
    Future.delayed(const Duration(milliseconds: 300), () {
      scrollToIndex(index);
      animationController.forward(from: 0);
    });
    targetRpid = null;
  }
}
```

### 高亮动画
```dart
AnimatedBuilder(
  animation: animationController,
  builder: (context, child) {
    final color = ColorTween(
      begin: Theme.of(context).colorScheme.primaryContainer.withOpacity(0.3),
      end: Colors.transparent,
    ).animate(CurvedAnimation(
      parent: animationController,
      curve: const Interval(0.5, 1.0),
    )).value;
    return Container(color: color, child: child);
  },
  child: child,
)
```

## 使用方法

### 查看楼中楼排序
1. 点击一楼评论的"共xx条回复"进入楼中楼
2. 顶部显示排序按钮,默认"按热度"
3. 点击按钮切换为"按时间"排序

### 查看对话
1. 在楼中楼页面,找到回复别人的评论(二楼)
2. 点击评论下方的"对话"按钮
3. 自动打开对话视图并定位到父评论

### 定位评论
1. 通过 `id` 参数打开楼中楼页面
2. 自动滚动到指定评论并高亮显示
3. 1秒后高亮效果淡出

## 测试建议

1. **排序测试**: 进入热门视频的评论区,查看楼中楼排序是否正常切换
2. **对话测试**: 找到有多层回复的评论,测试对话功能是否正确显示对话链
3. **定位测试**: 通过不同入口(通知、搜索等)进入评论,测试定位功能是否准确
4. **动画测试**: 观察高亮动画是否流畅,颜色过渡是否自然

## 注意事项

1. 对话功能仅在二楼评论中显示(parent != root)
2. 定位功能依赖评论的 rpid 字段
3. 高亮动画使用 GetSingleTickerProviderStateMixin,需要正确管理生命周期
4. 排序切换会重新加载评论列表,清空当前数据

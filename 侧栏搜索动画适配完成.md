# 侧栏搜索动画适配完成

## 问题描述
主页新增侧栏布局后，搜索框被替换为放大镜图标，导致原有的搜索框展开动画失效。

## 解决方案
为侧栏的搜索图标添加展开动画支持，从圆形图标位置展开到全屏搜索页，并优化圆角过渡效果。

## 修改内容

### 1. lib/common/widgets/search_expand_route.dart

**SearchExpandPageRoute 类**：
- 添加 `isCircular` 参数，用于标识源组件是否为圆形
- 默认值为 `false`（搜索框）

**_SearchExpandTransition 类**：
- 添加 `isCircular` 参数处理
- 优化圆角计算逻辑：
  - 圆形图标：从 `sourceRect.width / 2`（圆形半径）→ 屏幕圆角(35) → 0
  - 搜索框：从 25 → 屏幕圆角(35) → 0
- 保持透明度渐变动画不变

### 2. lib/pages/home/view.dart

**UserAndSearchVertical 类**：
1. 添加 `GlobalKey sidebarSearchKey` 用于捕获搜索图标位置
2. 为搜索图标按钮包裹 `Container` 并设置 key
3. 添加 `_navigateToSearch()` 方法：
   - 获取搜索图标的屏幕位置和大小
   - 使用 `SearchExpandPageRoute` 实现展开动画
   - 传递 `isCircular: true` 参数
   - 如果获取位置失败，降级为普通跳转

## 技术实现

### 圆角自适应逻辑

```dart
// 根据源组件类型计算初始圆角
final initialRadius = isCircular ? sourceRect.width / 2 : 25.0;

if (progress < 0.85) {
  // 前85%：从初始圆角过渡到屏幕圆角
  final t = progress / 0.85;
  borderRadius = initialRadius + (screenBorderRadius - initialRadius) * t;
} else {
  // 后15%：从屏幕圆角快速过渡到0
  final t = (progress - 0.85) / 0.15;
  borderRadius = screenBorderRadius * (1 - t);
}
```

### 侧栏搜索实现

```dart
// 1. 定义 GlobalKey
static final GlobalKey sidebarSearchKey = GlobalKey();

// 2. 包裹搜索图标
Container(
  key: sidebarSearchKey,
  child: IconButton(...),
)

// 3. 实现展开动画（标记为圆形）
void _navigateToSearch(BuildContext context) {
  final renderBox = sidebarSearchKey.currentContext?.findRenderObject() as RenderBox?;
  if (renderBox == null) {
    Get.toNamed('/search', parameters: {'hintText': ctr.defaultSearch.value});
    return;
  }

  final position = renderBox.localToGlobal(Offset.zero);
  final size = renderBox.size;
  final rect = Rect.fromLTWH(position.dx, position.dy, size.width, size.height);

  Navigator.of(context).push(
    SearchExpandPageRoute(
      sourceRect: rect,
      hintText: ctr.defaultSearch.value,
      isCircular: true, // 关键：标记为圆形图标
    ),
  );
}
```

## 动画效果

### 侧栏布局（圆形图标）
- 从放大镜图标位置展开
- 圆角从圆形（半径 = 宽度/2）→ 屏幕圆角(35) → 0
- 透明度渐变：0 → 1（前15%）
- 内容淡入：0 → 1（70%-100%）

### 普通布局（搜索框）
- 从搜索框位置展开
- 圆角从 25 → 屏幕圆角(35) → 0
- 透明度渐变：0 → 1（前15%）
- 内容淡入：0 → 1（70%-100%）

### 通用参数
- **动画时长**: 400ms 进入，300ms 退出
- **动画曲线**: easeOutCubic / easeInCubic

## 测试建议

1. 开启侧栏布局（左侧或右侧）
2. 点击侧栏的搜索图标
3. 观察是否从图标位置平滑展开到全屏
4. 测试返回动画是否流畅
5. 切换回普通布局，确认搜索框动画仍正常

## 相关文件

- `lib/pages/home/view.dart` - 侧栏搜索图标
- `lib/common/widgets/search_expand_route.dart` - 展开动画路由
- `搜索页展开动画完成.md` - 原始动画实现文档

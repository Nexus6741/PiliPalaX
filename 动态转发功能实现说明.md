# 动态转发功能实现说明

## 功能概述
为动态页面添加转发功能，用户可以转发任何动态并添加自己的评论。

## 实现内容

### 1. 转发页面
**文件**: `lib/pages/dynamics_repost/view.dart`

创建了完整的转发动态页面 `DynamicRepostPage`，包含：

#### 主要功能：
- **富文本编辑器**：支持输入文本、@用户、表情
- **原动态预览**：显示被转发动态的封面、作者和内容摘要
- **实时预览**：显示转发后的完整内容格式
- **转发按钮**：只有输入内容后才能转发

#### 核心方法：
```dart
// 获取转发内容（包含原动态的富文本）
List<Map<String, dynamic>>? _getRepostContent()

// 发布转发
Future<void> _onPublish()
```

### 2. HTTP API 修改
**文件**: `lib/http/dynamics.dart`

在 `createDynamic` 方法中添加了转发支持：

#### 新增参数：
- `dynIdStr`: 被转发的动态ID

#### 关键修改：
```dart
// 场景类型
'scene': dynIdStr != null
    ? 4 // 转发动态
    : images != null && images.isNotEmpty
        ? 2
        : 1,

// 转发源信息
if (dynIdStr != null)
  'web_repost_src': {
    'dyn_id_str': dynIdStr,
  },
```

### 3. 路由配置
**文件**: `lib/router/app_pages.dart`

添加了转发页面路由：
```dart
CustomGetPage(
    name: '/dynamics/repost',
    page: () => DynamicRepostPage(
          item: Get.arguments?['item'],
          dynIdStr: Get.arguments?['dynIdStr'],
          callback: Get.arguments?['callback'],
        ))
```

### 4. UI 集成
**文件**: `lib/pages/dynamics/widgets/action_panel.dart`

修改了动态卡片的转发按钮：
- 点击转发按钮打开转发页面
- 转发成功后自动刷新动态列表
- 显示转发数量

## 转发内容格式

转发动态的内容格式为：
```
[用户输入的内容]
//@原作者:原动态内容
```

API格式：
```json
{
  "contents": [
    // 用户输入的富文本内容
    {"raw_text": "用户评论", "type": 1, "biz_id": ""},
    // 转发标记
    {"raw_text": "//", "type": 1, "biz_id": ""},
    {"raw_text": "@原作者", "type": 2, "biz_id": "作者ID"},
    {"raw_text": ":", "type": 1, "biz_id": ""},
    // 原动态的富文本内容
    ...
  ]
}
```

## 使用流程

1. 用户在动态列表中点击"转发"按钮
2. 打开转发页面，显示原动态预览
3. 用户输入评论（可选，不输入则使用默认文本"转发动态"）
   - 支持纯文本
   - 支持@用户
   - 支持表情
4. 点击"转发"按钮（始终可用）
5. 显示"转发成功"提示
6. 自动返回并刷新动态列表

## 功能特性

### 1. 富文本支持
- **@用户**：点击@按钮选择用户
- **表情**：点击表情按钮选择表情
- **实时高亮**：@用户显示为蓝色

### 2. 原动态预览
- 显示原作者头像/封面
- 显示原作者名称（蓝色高亮）
- 显示原动态内容摘要（最多2行）

### 3. 智能内容提取
自动从不同类型的动态中提取：
- 视频动态：封面、标题
- 图文动态：图片、文本
- 番剧动态：封面、标题
- 纯文本动态：文本内容

### 4. 错误处理
- 网络异常提示
- 转发失败提示
- 防止重复提交

## 参考实现

本实现参考了 PiliPlus 的以下文件：
- `PiliPlus/lib/pages/dynamics_repost/view.dart` - 转发页面UI
- `PiliPlus/lib/http/dynamics.dart` - API实现
- `PiliPlus/lib/pages/dynamics/widgets/action_panel.dart` - 转发按钮

## 测试建议

### 1. 基本功能测试
- 转发纯文本动态
- 转发图文动态
- 转发视频动态
- 转发已转发的动态（二次转发）

### 2. 富文本测试
- 输入纯文本转发
- 使用@用户功能
- 使用表情功能
- 混合使用文本、@用户、表情

### 3. UI测试
- 原动态预览正确显示
- 转发按钮状态正确（有内容时可点击）
- 转发成功后列表刷新
- 表情和@面板正常切换

### 4. 边界测试
- 不输入内容直接转发（使用默认文本"转发动态"）
- 网络异常时的提示
- 转发失败时的提示
- 二次转发（转发已转发的动态）

## 注意事项

1. **转发格式**：必须按照B站API要求的格式构建转发内容
2. **场景类型**：转发动态的scene必须设置为4
3. **aigc参数**：转发时必须在option中添加 `aigc: 2`
4. **默认文本**：不输入内容时使用"转发动态"作为默认文本
5. **富文本解析**：需要正确解析原动态的富文本节点
6. **回调刷新**：转发成功后需要刷新动态列表
7. **用户体验**：转发页面支持@用户和表情，提升用户体验

## API 说明

### 转发动态API
- **端点**: `/x/dynamic/feed/create/dyn`
- **方法**: POST
- **关键参数**:
  - `dyn_req.scene`: 4（转发场景）
  - `dyn_req.option.aigc`: 2（必需）
  - `web_repost_src.dyn_id_str`: 被转发的动态ID
  - `dyn_req.content.contents`: 富文本内容数组

### 内容类型
- `type: 1` - 普通文本
- `type: 2` - @用户
- `type: 9` - 表情

## 后续优化建议

1. **图片转发**：支持在转发时添加图片
2. **话题转发**：支持在转发时添加话题
3. **定时转发**：支持定时发布转发
4. **转发统计**：显示转发数量的实时更新
5. **转发链**：显示完整的转发链路

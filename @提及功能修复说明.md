# @提及功能修复说明

## 问题描述
在动态发布功能中，@用户后发布的动态显示为普通文本，没有真正@到用户。

## 问题原因
之前的实现只是简单地将文本内容发送给API，没有按照B站API要求的格式处理@用户的富文本内容。

## 解决方案

### 1. 扩展 RichTextEditingController
在 `lib/common/widgets/rich_text/controller.dart` 中添加：

- **AtUserInfo 类**：存储@用户的信息（uid和username）
- **_atUserMap**：映射@文本到用户信息
- **getRichTextItems()**：解析文本内容，返回富文本项列表
- **insertAtUser()**：插入@用户的方法

### 2. 修改 HTTP API
在 `lib/http/dynamics.dart` 的 `createDynamic` 方法中：

- 添加 `richContent` 参数：接收富文本内容数组
- 优先使用 `richContent`，如果没有则使用普通 `content`

### 3. 更新控制器
在 `lib/pages/dynamics_create/simple_controller.dart` 中：

- `publishDynamic` 方法添加 `richContent` 参数
- 将富文本内容传递给API

### 4. 修改视图
在 `lib/pages/dynamics_create/simple_view_fixed.dart` 中：

- `_onMentionUser`：使用 `insertAtUser` 方法插入@用户
- `_onPublish`：解析富文本项并转换为API格式

## API 格式说明

根据PiliPlus的实现，B站动态API的富文本格式：

```json
{
  "content": {
    "contents": [
      {
        "raw_text": "普通文本",
        "type": 1,
        "biz_id": ""
      },
      {
        "raw_text": "@用户名",
        "type": 2,
        "biz_id": "用户ID"
      },
      {
        "raw_text": "[表情文本]",
        "type": 9,
        "biz_id": ""
      }
    ]
  }
}
```

### 类型说明
- `type: 1` - 普通文本
- `type: 2` - @用户（需要提供用户ID）
- `type: 4` - 投票
- `type: 9` - 表情

## 测试步骤

1. 打开动态发布页面
2. 点击@按钮，选择一个用户
3. 输入一些文本
4. 发布动态
5. 查看发布的动态，@用户应该显示为蓝色可点击链接

## 注意事项

- @用户时必须提供用户ID（uid），否则会显示为普通文本
- @用户的格式必须是 `@用户名 `（后面有空格）
- 富文本项的顺序很重要，需要按照文本中出现的顺序排列
